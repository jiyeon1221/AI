EM_scan:
  live:
    type: full
    method: IntADC
  cells:
    - fCanvasHeatmap
    - fCanvas_Tower${current_tower}

calib_scan:
  live:
    type: full
    method: IntADC
  cells:
    - fCanvasHeatmap
    - fCanvas_Tower${current_tower}

hv_equalization:
  live:
    type: full
    method: PeakADC
  cells:
    - fCanvas_Tower${current_tower}

hv_equalization_sim:
  live:
    type: full
    method: PeakADC
  cells:
    - fCanvas_Tower${current_tower}

position_scan:
  live:
    type: full
    method: PeakADC
  cells:
    - fCanvas_Tower${current_tower}

position_scan_sim:
  live:
    type: full
    method: PeakADC
  cells:
    - fCanvas_Tower${current_tower}
